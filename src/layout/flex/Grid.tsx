import styled from "styled-components";
import Base from "./Base";

export const Grid = styled(Base).attrs({ className: 'grid' })`
    margin: 5px;
    padding: 5px;
    *{
        box-sizing: border-box;
    }
`
