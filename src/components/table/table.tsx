import React from "react";
import { JSONObject } from "../../types";

interface TableProps {
    data: JSONObject
}

const mapData = (data: JSONObject) => {
    Object.keys(data).forEach((v) => {
        
    })
}

export const Table = () => {};
