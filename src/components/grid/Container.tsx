import React, { ReactElement } from 'react'
import styled from 'styled-components'

const ContainerStyle = styled.div.attrs({ className: 'portal-container'} )`
    width: 100%;
`;

interface Props {
    
}

export function Container(props: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
